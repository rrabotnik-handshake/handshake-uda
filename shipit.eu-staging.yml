machine:
  environment:
    KUBE_APP_NAME: REPLACE_WITH_SERVICE_NAME
    KUBE_CLUSTER: eu-staging
    GITHUB_REPO_NAME: REPLACE_WITH_SERVICE_NAME
    DISABLE_SCHEMA_MIGRATIONS: "true"
    USE_ARGOCD: "true"

ci:
  require:
    - "buildkite/REPLACE_WITH_SERVICE_NAME"
  hide:
    - shipit/staging

status:
  context: "shipit/eu-staging"
  delay: 1m
